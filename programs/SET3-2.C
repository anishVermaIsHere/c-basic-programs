#include<stdio.h>

main()
{
int a,b,c,i,j,n;
char ch[20];
 clrscr();


 printf("\n enter the characters in the array");
 scanf("%s",&ch);
 for(i=0; ch[i]!='\0'; i++)
 {
 }
 printf("\n the length of the string is %d",i);
 printf("\n no. of comparison is %d",i);
 getch();
 }
