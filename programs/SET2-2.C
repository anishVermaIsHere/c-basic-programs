#include<stdio.h>
#include<time.h>

main()
{
int i,j,k,t,p,n;
 double p;
 time_t start,end;
 time(&start);
for(i=0; i<n; i++)
{
	for(j=0; j<n; j++)
	{
		for(k=0; k<n; k++)
		{   printf("\n first");
		}
	}
}

 time(&end);
 t=difftime(end,start);
 printf("\n%d",p);

 }